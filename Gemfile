source 'https://rubygems.org'

gem 'jekyll', '~> 4.4'

# Common plugins (optional, safe to include)
gem 'jekyll-feed', '~> 0.16'
gem 'jekyll-seo-tag', '~> 2.8'

# If you later need to add more plugins (e.g., sitemap), add them here and run `bundle install` locally.
